import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kuznetsov.dev',
  appName: 'pet-vk-player',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
